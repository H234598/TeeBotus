# Adapter, LLM and tool dependencies are pinned in pyproject.toml and
# adapter-dependencies.lock. Install them with:
#
#   python3 scripts/install_adapter_deps.py
#
# This cannot be represented as one normal pip resolver input because
# nio-bot 1.0.2.post1 declares matrix-nio==0.20.*, while TeeBotus' verified
# runtime contract uses matrix-nio==0.25.2 plus h11==0.16.0. On Python 3.14,
# litellm==1.83.7 also declares python-dotenv==1.0.1 while TeeBotus validates
# python-dotenv==1.2.2 through a sequenced install.
psycopg[binary]==3.3.4
