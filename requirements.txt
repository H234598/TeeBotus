# Adapter dependencies are pinned in adapter-dependencies.lock.
# Install them with:
#
#   python3 scripts/install_adapter_deps.py
#
# This cannot be represented as a normal pip resolver input because
# nio-bot 1.0.2.post1 declares matrix-nio==0.20.*, while TeeBotus'
# verified runtime contract uses matrix-nio==0.25.0 plus h11==0.16.0.
